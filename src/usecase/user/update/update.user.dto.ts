export interface InputUpdateUserDto {
  id: string;
  name: string;
  gameUserId: string;
  email: string;
}
export interface OutputCreateUserDto {
  id: string;
  name: string;
  gameUserId: string;
  email: string;
}
